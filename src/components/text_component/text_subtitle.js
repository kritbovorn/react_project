import React from "react";
import { Text ,View} from 'react-native'

const   TextSubTitle = ({text}) => {
  return (  
      <Text style={{color: '#E76F51', fontSize: 15}}>{text}</Text>
  );
}

export default TextSubTitle;

